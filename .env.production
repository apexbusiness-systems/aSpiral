# ════════════════════════════════════════════════════════════════
# aSpiral Production Environment Variables
# ════════════════════════════════════════════════════════════════
#
# ⚠️  CRITICAL: VITE_SUPABASE_URL and VITE_SUPABASE_PUBLISHABLE_KEY
#     must match the ACTIVE Supabase project in the APEX OMNiLiNK org.
#
# CORRECT PROJECT REF (from supabase/config.toml + JWT payload):
#   eqtwatyodujxofrdznen  (second char is 'q', NOT 'g')
#
# PRIOR BUG (fixed here): .env.production had 'egtwatyodujxofrdznen'
#   which does not exist — caused net::ERR_NAME_NOT_RESOLVED in production.
#
# After provisioning the Supabase project, replace PLACEHOLDER values
# with real credentials from Supabase Dashboard → Settings → API.
# Then also update: Cloudflare Pages Dashboard → Variables and secrets.
# ════════════════════════════════════════════════════════════════

VITE_SUPABASE_URL=https://eqtwatyodujxofrdznen.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=REPLACE_WITH_REAL_ANON_KEY_AFTER_PROJECT_PROVISIONING

VITE_ENABLE_WEBGL=true
VITE_APP_URL=https://aspiral.icu
